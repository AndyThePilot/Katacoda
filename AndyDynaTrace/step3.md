Install ActiveGate to enable dediacted K8S monitoring

Go to the Dynatrace SaaS -> Deployment status  -> Install ActiveGate -> Linux and copy the 'wget...' command.

Change permissions

`chmod +x Dyn*`{{execute}}

Run the installer

`./Dyn*`{{execute}